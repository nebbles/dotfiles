# This loads the 1password-cli plugin confiuration into the shell environment,
# enabling it to automatically alias common commands and provide the appropriate
# credentials directly from 1Password. It does this by providing them as
# environment variables to sub processes.
# https://developer.1password.com/docs/cli/get-started#install
# https://blog.1password.com/1password-cli-2_0/

#source /Users/bengreenberg/.config/op/plugins.sh
